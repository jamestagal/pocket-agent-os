# Expertise Index
# Auto-generated by meta-expert - Last updated: {{timestamp}}
#
# This file maps domains to their expertise files and provides project overview.

project:
  name: "{{project_name}}"
  description: "{{project_description}}"
  bootstrapped: "{{timestamp}}"

domains:
  # Each domain tracks a major area of the codebase
  # Uncomment and configure relevant domains for your project
  
  # - name: frontend
  #   file: frontend.yaml
  #   description: "UI components, pages, client-side logic"
  #   key_paths:
  #     - src/routes/
  #     - src/lib/components/
  
  # - name: api
  #   file: api.yaml
  #   description: "Server endpoints, business logic"
  #   key_paths:
  #     - src/lib/server/
  #     - src/routes/api/
  
  # - name: database
  #   file: database.yaml
  #   description: "Data models, queries, migrations"
  #   key_paths:
  #     - prisma/
  #     - src/lib/db/

tech_stack:
  # Populated from agent-os/product/tech-stack.md
  frontend: "{{frontend_framework}}"
  backend: "{{backend_framework}}"
  database: "{{database}}"
  language: "{{primary_language}}"

specialists:
  # Map domains to specialist agents (defaults to 'implementer' if not specified)
  # These are used by smart routing to delegate tasks appropriately
  # 
  # frontend: sveltekit-specialist
  # backend: implementer
  # database: implementer
